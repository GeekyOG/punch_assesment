import React from "react";

function FooterHead({ children }) {
  return (
    <p className="lg:text-[0.875rem] font-[600] text-[#FFFFFF9F] text-[0.75rem]">
      {children}
    </p>
  );
}

export default FooterHead;
